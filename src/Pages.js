import React from "react";
import { MapPage, DashboardPage, HomePage } from "./Components";

// These are already exported from Components.js, so this file is minimal
export { MapPage, DashboardPage, HomePage };
